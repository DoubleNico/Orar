/**
 * Represents the data associated with a refresh token.
 *
 * @property {string} userId - The unique identifier of the user.
 * @property {string} refreshTokenId - The unique identifier of the refresh token.
 */
export type RefreshTokenData = {
  userId: string
  refreshTokenId: string
}
